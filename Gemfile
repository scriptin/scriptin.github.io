source "https://rubygems.org"

# Jekyll and the rest of the dependencies will be installed transitively.
# This is the only gem we need to fully emulate GitHub Pages setup.
gem "github-pages", "~> 232", group: :jekyll_plugins
